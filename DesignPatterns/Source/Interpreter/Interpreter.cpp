// Design problem
// Inflexible expression of input processing

// Interpreter intent
// Given a language, define  a representation for its grammar, along with interpreter that uses the representation to interpreter 
#include "Main.h"
int main(int argc, char* argv[]) {
  
  //_8bit::My8bit m8b(0);
  //m8b.set_bit(3,true);
  //m8b.test();
  //project_manager::ProjectsManager pm(argc,argv);
  byte::test::Test(argc, argv);

  return 0;
}